spring:
  datasource:
    url: jdbc:h2:file:./.local/fixgo;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: false
fixgo:
  auth:
    local-key-file: .local/jwt.key
