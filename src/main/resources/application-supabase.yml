# Profile "supabase": same datasource contract as the base file; kept so existing run
# configurations (--spring.profiles.active=supabase) keep working. All values come from env vars.
spring:
  datasource:
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:5}
